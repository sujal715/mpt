# Render.com Build Command - Updated for deployment fix
export MAVEN_OPTS="-Xmx1024m -XX:MaxPermSize=256m"
cd clean-spring-boot && ./mvnw clean package -DskipTests -Dmaven.test.skip=true

# Render.com Start Command - Updated for deployment fix
cd clean-spring-boot && java -Xmx512m -jar target/mpt-0.0.1-SNAPSHOT.jar --spring.profiles.active=render
